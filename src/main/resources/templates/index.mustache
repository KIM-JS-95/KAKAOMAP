<!DOCTYPE HTML>
<html>
<head>
    <title>스프링부트 웹서비스</title>

    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>

<h1>스프링부트로 시작하는 웹 서비스</h1>

<div class = "col-md-12">
    <div class = "row">
        <div class = "col-md-6">
        </div>
    </div>
    <br>

        <!-- 검색 부분 -->
        <div class="search">
            <input type="text" placeholder="검색어를 입력해주세요." id="station">
        </div>

        <div class="button">
            <button>검색</button>
        </div>

    <!-- 목록 출력 영역 -->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>게시글번호</th>
            <th>주소</th>
            <th>충전기 타입</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td>{{add}}</td>
                <td>{{type}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
</div>

</body>
</html>
